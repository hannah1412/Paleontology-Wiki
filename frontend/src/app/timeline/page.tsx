import TimelinePage from '@/components/Timeline/TimelinePage';

export const metadata = {
  title: "Timeline",
  description: "Browse through the history of dinosaurs.",
};

export default function Page() {
  return (<TimelinePage/>);
}

